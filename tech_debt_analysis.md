# 技術債與代碼問題分析

## 1. 代碼冗餘 (Redundancy)
- **多語言實現方式**：目前採用完全獨立的 HTML/CSS/JS 檔案來處理中英文（如 `blog_zh_index.html` vs `blog_en_index.html`）。這導致維護困難，任何 UI 改動都需要修改兩套檔案。
- **重複的 JS 邏輯**：`blog_zh_script.js` 中存在多個相似的事件監聽器（如 `search-btn1` 和 `search-btn2` 的邏輯幾乎一致）。
- **內聯樣式**：部分 HTML 中存在內聯樣式，應統一移至 CSS。

## 2. 性能問題 (Performance)
- **圖片加載**：目前沒有圖片懶加載（Lazy Loading）機制。
- **字體加載**：字體檔案較大且直接在 CSS 中定義，可能導致 FOUT（無樣式字體閃爍）。
- **數據讀取**：`blog_zh_script.js` 通過循環 fetch 15 個檔案來獲取貼文，效率低下且難以擴展。

## 3. 代碼規範與結構 (Code Quality)
- **缺乏模組化**：JS 代碼過長且混合了 UI 交互與數據處理邏輯。
- **註解不規範**：部分註解較為隨意，缺乏統一格式。
- **硬編碼**：貼文數量（15）是硬編碼的，增加新貼文需要修改代碼。

## 4. 功能缺失 (Missing Features)
- **動態語言切換**：目前切換語言需要跳轉頁面，無法保留當前狀態。
- **背景調整**：缺乏用戶自定義背景顏色的功能。

## 5. 數據解析 (Data Parsing)
- **自定義格式解析**：使用 `<註解>` 和 `<範例>` 標籤的自定義解析邏輯較為脆弱，建議優化解析器或改用更標準的格式（如 JSON/Frontmatter）。
